import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  TextInput,
  FlatList,
  ActivityIndicator,
} from "react-native";
import { useState, useEffect } from "react";

export default function Account({ navigation }) {
  return (
    <View>
      <Text>Đang là trang Tài khoản </Text>
    </View>
  );
}
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
